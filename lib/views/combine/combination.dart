import 'package:flutter/material.dart';

class CombinationPage extends StatelessWidget {
  const CombinationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Combine items'),
      ),
      body: const Text('In group'),
    );
  }
}
